部分 QMS 查询失败时，页面此前将失败数据集替换为空数组并显示同步成功；服务端限制单页返回数量时也可能提前结束分页。本次改为完整快照更新，补充请求超时、响应校验、分页完整性检查和工厂隔离缓存，避免错误统计。

同时分离页面样式和脚本，增加月份/趋势请求序列保护、Node 回归测试与 CI。保留现有线上业务公式及 Cloudflare 配置。

验证：Node 语法检查、静态资源和前端凭证检查、回归测试、静态构建、git diff --check。浏览器验证记录见 docs/VALIDATION.md。

影响：发布必须包含 assets/，不能再只更新 index.html。必须先核实凭证轮换及 QMS 指标口径，并完成真实数据对账；这些事项未完成前不可认定为正式验收通过。所有者已授权在不更改 QMS 密码的前提下清理当前源码并发布；历史记录未重写。
