接管修复以线上 gh-pages 版本为基线，将 main 旧 HTML 中的明文 QMS 凭证代码移除，继续使用现有 Worker 代理，不更改 QMS 密码、Secret 或历史记录。

修复供应商页面、弹窗、图表、风险原因与空状态的语言切换。供应商大屏改为视口固定行高，300 行风险数据与 1 行筛选结果时下方三块高度一致。来料大屏限制每页 20 条，通过合成层动画滚动并自动翻页，后台暂停，尊重减少动态效果设置。另包括请求超时、分页完整性、缓存隔离和竞态修复。

验证：18 项 Node 测试、全部业务脚本语法和资源检查、静态构建及 git diff --check 通过。浏览器用 6000 条虚构记录测试中/英/泰/中泰文案、300→1 行固定高度、1500 条待办的有界 DOM 和手动分页；真实 QMS 只读连接已验证。

维护：分离源码、补充 CI 与 Pages 工作流。合并后 Pages 改由 main 受检 dist 产物发布，旧 gh-pages 仅保留历史。源码清理不等于历史凭证已失效；用户明确要求保留现有密码。既有业务公式不变，口径对账和大工厂汇总优化不在本 PR 中。
