# 2026-09-19 验证记录

## 恢复原始 UI 与显示修复

开发分支 `codex/restore-original-ui`。

- 移除两种附加主题、透明度控制及相关脚本；原始背景为 `rgb(243, 246, 250)`，卡片为白色，设置只含四种语言，原主题偏好不再被读取。
- 复现 1280×720 供应商看板图表被普通断点压扁、风险卡片另起一行；修复后中泰模式下方三块同一行、均高 179px，1920×1080 英文模式均高 351px，无页面溢出。
- 来料大屏表格宽度与容器一致（小屏均为 1231px），所有列均在视口内，滚动行高保持 44px；统计与检验员文字不再重叠。
- 测试数据扩展为 7 名检验员，全部柱形可在内部滚动查看，总数正确为 1500（原实现只累计前五名，显示 1072）；新增超过五人及未分配人员回归测试。
- 普通总览双语日期、刷新及导出按钮文字无溢出；中英泰及双语语言设置、任务详情弹窗正常，Chrome 控制台无错误或警告。
- 31 项 Node 测试、静态及前端凭证检查、生产构建、差异检查通过。移除三项已退役主题功能的测试。

## 纯色背景

开发分支 `codex/remove-gradient-backgrounds`。

- 三种主题全 DOM（含 before/after 伪元素）计算样式检查未发现渐变背景；业务 HTML、CSS、JS 中无渐变定义。
- 普通透明在 0% 与 100% 时分别为不透明和完全透明；两种大屏截图检查通过，供应商页面高度等于视口，控制台无错误或警告。
- 环形统计改用纯色 SVG：4500 批记录正确显示合格 66.7%、不合格 33.3%，来料大屏仍只挂载可见行。新增分段比例、零项跳过、单类满环和空数据测试。
- 33 项 Node 测试、静态及前端凭证检查、生产构建、差异检查通过。

## 显示设置与可调透明度

开发分支 `codex/display-settings`。

- 32 项 Node 测试通过，新增首屏偏好恢复、零透明度、无效设置及存储不可用、预览与持久化、上下限覆盖；静态与前端凭证检查、构建、差异检查通过。
- Chrome 验证右下角面板打开、Escape 和点击外部关闭；中、英、泰、中泰文案及选择状态正确。
- 普通透明在 0% 时卡片底色完全不透明、100% 时完全透明，0.1% 精细调节正常，文字 opacity 保持 1；普通透明背景无渐变高光及模糊，经典主题禁用滑杆。
- 普通预览设置为普通透明 / 69.9% 后刷新，主题、透明度与语言正确恢复。
- 来料大屏保持可见行窗口（测试视口 8 行），玻璃卡片无背景模糊；1366×768 中泰供应商大屏中面板完整显示，页面不溢出，设置与退出按钮不重叠。
- 两种大屏与普通页面视觉检查、浏览器控制台检查通过，无错误或警告。

## 液态玻璃主题

半透明增强（`codex/glass-transparency`）：主要卡片白色底层不透明度为 16%–42%，普通模式与来料大屏计算样式一致；Chrome 截图检查背景透色、表格和结果统计可读性，控制台无错误。29 项回归测试、静态检查、构建与差异检查通过。

开发分支 `codex/liquid-glass-theme`。

- 现有 29 项回归测试、静态与前端凭证检查、生产构建及 `git diff --check` 通过。
- Chrome 验证主题菜单切换并收起、中文与英文主题名称更新、正常预览刷新后恢复玻璃主题。
- 质量总览、供应商大屏和工厂弹窗视觉检查通过；供应商下方三块等高，大屏无页面溢出。
- 来料大屏仍只渲染可见行窗口，玻璃主题下大屏卡片 `backdrop-filter` 为 `none`；浏览器控制台无错误。

## 连续循环与界面优化

开发分支 `codex/continuous-wallboard-ui`。

- 29 项 Node 测试通过，覆盖 1500 条任务逐条到达及首尾衔接、60/120 Hz 下 72px/s 一致、刷新后位置保持、暂停恢复、缩放、减少动态效果和旧任务路由兼容。
- Chrome 隔离数据：1500 条待办在 898px 视口仅绘制 9 行，1366×768 中泰模式仅绘制 6 行；无分页、无行分隔线，内容按任务顺序滚动。
- 语言菜单选择后收起，英文与中泰文案正确；单条任务静止、空结果正常，退出后普通总览恢复分页。
- 1366×768 中泰模式统计卡片完整显示环图、总批次、三类数量和占比，页面高度等于视口高度；浏览器控制台无错误。
- 静态检查、前端凭证检查、生产构建及 `git diff --check` 通过。

## 来料看板滚动修复

开发分支 `codex/fix-wallboard-scroll-lines`。

- 来料大屏使用独立单元格边框、零边框间距，让分隔线与 `tbody` 的合成层一起移动；普通表格样式保持原样。
- 来料滚速由 28 调整为 36 像素/秒，继续使用按时间计算的亚像素位移；悬停时不再调度空闲动画帧。
- Node 回归测试共 22 项通过，新增 60/120 Hz 滚速一致、悬停恢复无跳跃、页尾暂停后单次翻页、减少动态效果四项覆盖。
- 静态检查、前端凭证检查、生产构建、`git diff --check` 均通过。
- Chrome 隔离数据场景：1500 条任务、每页 20 条，滚动中的分隔线跟随数据行，表头位置保持不变；悬停位置保持不变，移出恢复；手动翻页重置滚动位置。

## 维护基线

基线为 gh-pages 3ddea818，开发分支 codex/iqc-stability。

- node scripts/check.mjs：业务脚本语法、静态资源引用、前端凭证检查通过。
- node --test tests/*.test.cjs：18 项通过。
- node scripts/build.mjs：通过，生产文件显式白名单，不包含测试数据与文档。
- git diff --check：通过。
- Chrome：中、英、泰、中泰供应商界面、图表、风险原因、空态正确更新。
- 6000 条虚构记录，300 家风险供应商筛选至 1 家时，中泰大屏下方三块高度均约 265px，不随行数变化；页面高度与 898px 视口一致。
- 来料看板 1500 条待办仅挂载 20 条；768px 视口页面不溢出，检验员区域可用。
- 自动化测试覆盖滚动时无逐帧布局读取、隐藏暂停、恢复、退出清理、最后一页数据可达。
- 浏览器控制台无错误；本地真实 QMS 数据同步成功。

当前记录不声称所有工厂、年度大数据量或所有设备均已全面验收。QMS 历史凭证轮换未知；所有者明确要求本次仅清理当前源码，不改密码。
