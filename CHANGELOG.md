# 变更记录

## 未发布

- 增强液态玻璃半透明效果：主要表面的白色不透明度降至 16%–42%，普通页面和大屏保持一致，强化背景透色并加深辅助文字。
- 新增液态玻璃主题：渐变背景、半透明卡片、柔和高光与背景模糊；保留经典主题，支持跨页面记忆和多语言菜单。大屏采用无背景模糊的轻量表面以保持滚动性能。
- 来料大屏改为全部任务连续循环，取消分页与行分隔线，速度提升至 72 像素/秒；通过可见行窗口控制 DOM 数量，刷新保留当前位置。
- 移除“质量管理员”和独立检验任务导航，旧任务链接返回质量总览；语言选项收纳为展开菜单。
- 放大检验结果环图与总批次，以三行颜色区分的数字突出合格、特采、不合格批次数和占比。
- 修复来料看板自动滚动时行分隔线与内容错位；滚速由 28 调整为 36 像素/秒，悬停时停止动画帧调度。
- 将 README 整理为正式项目首页，新增维护指南，集中记录统计口径、发布回退与运维信息。
- 从线上 gh-pages 建立维护基线，分离 HTML、CSS、JavaScript。
- 修复服务端限制 pageSize 时漏页；无效/重复/缺失分页明确失败。
- 请求支持超时、正确识别 HTTP 错误和数字状态码。
- 总览部分失败不再以零数据覆盖旧快照或误报成功。
- 缓存按工厂、版本和有效期校验，保留待办与上月统计。
- 月份、工厂、趋势和详情切换防止过期响应回写。
- 图表空态后重建实例；无退货/特采时明确显示暂无不合格批次。
- 补充 Node 回归测试、CI、构建脚本和维护说明。
- 所有者要求保留 QMS 密码，仅移除当前源码凭证；历史泄露风险不因当前文件清理而消失。

## 2026-09-19 看板维护

- 补齐供应商、图表、风险原因、弹窗、筛选和空态多语言，保留用户语言选择。
- 修复供应商看板内联 display 覆盖布局，使用固定视口行高，列表在卡片内部滚动。
- 来料看板每页最多 20 条、自动轮播及手动翻页，滚动使用 transform，隐藏页面暂停，退出释放资源。
- 新增 Pages 构建发布流程，main 作为正式来源，生产产物排除测试与文档。
- 保留 QMS 与 Worker 原配置，当前源码不包含旧登录凭证，历史未改写。
